

gr_edit .yaxis1.title.style.editstyle size(small) editcopy
// title size

// title size

gr_edit  .style.editstyle margin(vsmall) editcopy
// Handle_Times margin

gr_edit  .xaxis1.style.editstyle majorstyle(tickstyle(textstyle(size(vsmall)))) editcopy
gr_edit  .xaxis1.style.editstyle majorstyle(use_labels(no)) editcopy
gr_edit  .xaxis1.style.editstyle majorstyle(alternate(no)) editcopy
// xaxis1 edits

gr_edit  .xaxis1.style.editstyle majorstyle(tickstyle(textstyle(size(tiny)))) editcopy
gr_edit  .xaxis1.style.editstyle majorstyle(use_labels(no)) editcopy
gr_edit  .xaxis1.style.editstyle majorstyle(alternate(no)) editcopy
// xaxis1 edits

gr_edit  .xaxis1.style.editstyle majorstyle(tickstyle(textstyle(size(tiny)))) editcopy
gr_edit  .xaxis1.style.editstyle majorstyle(use_labels(no)) editcopy
gr_edit  .xaxis1.style.editstyle majorstyle(alternate(no)) editcopy
// xaxis1 edits

gr_edit  .xaxis1.reset_rule 15, tickset(major) ruletype(suggest) 
// xaxis1 edits

// xaxis1 edits

gr_edit  .xaxis1.reset_rule , tickset(major) ruletype(minmax) 
// xaxis1 edits

gr_edit  .xaxis1.reset_rule 15, tickset(major) ruletype(suggest) 
// xaxis1 edits

gr_edit  .xaxis1.reset_rule 15, tickset(major) ruletype(suggest) 
// xaxis1 edits

gr_edit  .xaxis1.title.style.editstyle size(vsmall) editcopy
// title size

// title size

gr_edit  .legend.Edit, style(labelstyle(size(tiny)))
// legend size

gr_edit  .legend.Edit, style(labelstyle(size(vsmall)))
// legend size

// legend size

gr_edit  .note.draw_view.setstyle, style(no)
// note edits

gr_edit  .title.draw_view.setstyle, style(no)
// title edits

gr_edit .yaxis1.style.editstyle majorstyle(tickstyle(textstyle(size(tiny)))) editcopy
gr_edit .yaxis1.style.editstyle majorstyle(use_labels(no)) editcopy
gr_edit .yaxis1.style.editstyle majorstyle(alternate(no)) editcopy
// yaxis1 edits

gr_edit .yaxis1.style.editstyle majorstyle(tickstyle(textstyle(size(tiny)))) editcopy
gr_edit .yaxis1.style.editstyle majorstyle(use_labels(no)) editcopy
gr_edit .yaxis1.style.editstyle majorstyle(alternate(no)) editcopy
// yaxis1 edits

gr_edit .yaxis1.style.editstyle majorstyle(tickstyle(textstyle(size(vsmall)))) editcopy
gr_edit .yaxis1.style.editstyle majorstyle(use_labels(no)) editcopy
gr_edit .yaxis1.style.editstyle majorstyle(alternate(no)) editcopy
// yaxis1 edits

gr_edit .yaxis1.style.editstyle majorstyle(tickstyle(textstyle(size(vsmall)))) editcopy
gr_edit .yaxis1.style.editstyle majorstyle(use_labels(no)) editcopy
gr_edit .yaxis1.style.editstyle majorstyle(alternate(no)) editcopy
// yaxis1 edits

gr_edit .legend.plotregion1.label[3].text = {}
gr_edit .legend.plotregion1.label[3].text.Arrpush Trend of the Average
// label[3] edits

// label[3] edits

gr_edit .legend.plotregion1.label[2].draw_view.setstyle, style(no)
// label[2] edits

gr_edit .legend.plotregion1.label[2].draw_view.setstyle, style(yes)
// label[2] edits

// label[2] edits

// <end>

